# 네오클라우드 인사이트 📊

오라클·코어위브·네비우스·아이렌 등 **네오클라우드(AI 인프라) 기업**을 추종하는
투자자용 모바일 대시보드입니다. Toss 스타일의 심플·클린한 UI로, 핵심 지표와
실시간 뉴스를 한 화면에서 빠르게 파악할 수 있습니다.

## ✨ 주요 기능

- **홈 대시보드** — 회사별 카드(주가·등락률·수주잔고·확보용량), 미니 스파크라인,
  섹터 합산 요약. 등락률/수주잔고/확보용량 기준 정렬.
- **🔔 급등락 알림** — 임계치(±3/5/10%)를 넘어 움직인 종목을 홈 상단에 모아 표시.
  관심종목을 우선 노출.
- **비교** — `추이`/`밸류에이션` 두 모드.
  - 추이: 시간(X축) 대비 **수주잔고($B)·확보 용량(GW)·매출($B)**(Y축)을 겹쳐 비교 + 랭킹 바.
  - 밸류에이션: **전력 1GW당 가치($B/GW)·시총/수주잔고·P/S·수주 커버리지(년분)**를
    매력 순으로 정렬해 저평가 종목을 한눈에.
- **📱 PWA** — '홈 화면에 추가'로 앱처럼 설치/실행(매니페스트·아이콘 포함).
- **🌙 다크모드** — 홈 우상단 토글로 라이트/다크 전환(시스템 설정 자동 감지·기억).
- **실시간 뉴스** — Google News에서 회사별/섹터 최신 헤드라인을 자동 검색
  (API 키 불필요).
- **종목 상세** — 기업 소개, 핵심 지표(시총·매출·YoY 성장률·수주잔고·확보용량·
  **파이프라인**·**가동률**·ARR), 분기별 추이 차트(잔고/용량/매출), 주가 촉매·리스크,
  주요 고객, 데이터 출처 링크, 실시간 뉴스.
- **관심종목** — 별표로 담아두면 한곳에 모아보기(localStorage).

## 🏢 추종 기업 (8곳)

| 회사 | 티커 | 분류 | 핵심(2026-06 기준) |
| --- | --- | --- | --- |
| 오라클 | ORCL | 하이퍼스케일 | RPO ~$553B, OpenAI ~$300B 계약 |
| 코어위브 | CRWV | 네오클라우드 | 백로그 ~$99B, 3.5GW, Meta $21B |
| 네비우스 | NBIS | 풀스택 | MS 최대 $19.4B, 2.5GW 목표 |
| 아이렌 | IREN | 자체전력 | MS $9.7B·NVDA $3.4B, 4.5GW+ |
| 어플라이드 디지털 | APLD | HPC 호스팅 | CoreWeave 400MW·$11B 임대 |
| 사이퍼 마이닝 | CIFR | HPC 전환 | Fluidstack 224MW·$3.8B(구글 백스톱) |
| 크루소 | 비상장 | Stargate | 아빌린 2.1GW, 밸류 $10B+ |
| 람다 | 비상장 | GPU 클라우드 | MS 수십억 달러, 밸류 ~$6B |

> 비상장(크루소·람다)은 실시간 주가 대신 밸류에이션을 표시하며, 뉴스는 정상 검색됩니다.

> 한국 증시 관례에 맞춰 **상승=빨강, 하락=파랑**으로 표시합니다.

## 🚀 실행

```bash
npm install
npm run dev
# http://localhost:3000
```

빌드:

```bash
npm run build && npm start
```

## 🧱 기술 스택

- **Next.js 14** (App Router) + **React 18** + **TypeScript**
- **Tailwind CSS** (Toss 팔레트), **Pretendard** 폰트
- **Recharts** (비교 차트)
- 서버 API 라우트로 실시간 데이터 조회 (외부 키 불필요)

## 🔌 데이터 출처

| 데이터 | 출처 | 비고 |
| --- | --- | --- |
| 주가·등락률 | `/api/quote` → Yahoo Finance | 60초마다 갱신, 실패 시 폴백가 |
| 뉴스 | `/api/news` → Google News RSS | 회사명/섹터 키워드 검색 |
| 수주잔고(RPO)·확보용량(GW) | `lib/data.ts` 큐레이션 | 실적발표·공시 기반(참고용) |

수주잔고와 전력·용량 데이터는 공개 API로 제공되지 않아, 실적발표/공시를 토대로
`lib/data.ts`에 직접 큐레이션했습니다. **분기 실적이 나올 때마다 해당 파일의
시계열만 갱신**하면 차트가 자동 반영됩니다.

## 🏢 추종 기업 추가/수정

`lib/data.ts`의 `COMPANIES` 배열에 객체를 추가하면 모든 화면(홈/비교/뉴스/상세)에
자동 반영됩니다. 티커(`symbol`)만 정확하면 실시간 주가·뉴스가 연결됩니다.

## ⚠️ 면책

본 앱의 모든 수치는 참고용이며 투자 권유가 아닙니다. 큐레이션된 수주잔고·용량
값은 실제와 차이가 있을 수 있으니, 투자 판단 전 원문 공시를 확인하세요.
